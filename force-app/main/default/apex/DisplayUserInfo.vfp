<apex:page >
    <apex:pageBlock title="DisplayUserInfo">
        <apex:pageBlockSection columns="1">
            {! $User.firstname}{! $user.lastname}
            <BR />
            <P>
           
                  Date is: {! day(today())}/{! month(today())}/{! year(today())}
            </P>
            {! $User.FirstName & ' ' & $User.LastName }
           ({! $User.Username })
        </apex:pageBlockSection>
    </apex:pageBlock>
</apex:page>